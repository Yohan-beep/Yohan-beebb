import Scene from './Scene.js';
export default class Ending extends Scene {
    constructor(maxX, maxY, bgsrc, data) {
        super(maxX, maxY, bgsrc);
    }
    processInput(keyListener) {
    }
    update(elapsed) {
        return 0;
    }
    render(canvas) {
    }
}
//# sourceMappingURL=Ending.js.map